dist: trusty

language: java
addons:
  chrome: stable

cache:
  directories:
    - $HOME/.gradle

jdk:
  - openjdk11

before_install:
  - curl -s https://aerokube.com/cm/bash | bash && ./cm selenoid start --vnc --tmpfs 128

script:
  - ./gradlew clean test